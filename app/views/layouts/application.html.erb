<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Bbq" %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_pack_tag "application", media: "all" %>

    <%= javascript_pack_tag "application" %>
  </head>
  <body>
    <div class=" navbar navbar-expand-lg navbar-light bg-light navbar-default navbar-static-top">
      <div class="container">   
        <div class="navbar-collapse collapse navbar-responsive-collapse">
      
          <ul class="nav navbar-nav">
            <li><%= link_to "BBQ", root_path, class: 'navbar-brand' %></li>
            <% if user_signed_in? %>
            <li><%= link_to 'Профиль', current_user, class: 'navbar-brand' %>
              <%= link_to 'Выйти', destroy_user_session_path, :method => :delete, class: 'navbar-brand' %></li>
            <% else %>
            <li><%= link_to 'Войти', new_user_session_path, class: 'navbar-brand' %>
              <%= link_to 'Зарегистрироваться', new_user_registration_path, class: 'navbar-brand' %></li>
            <% end %>
      </ul>
    </div>
  </div>
</div>

<div class="container p-3">
  <div class="row">
    <div class="col-lg-9">     

        <%= yield %>
    </div>
  </div>
      <footer>
        <p>&copy; Company 2022</p>
      </footer>

</div>

  </body>
</html>